export enum EnumUserRole {
  admin = 'admin',
  user = 'commonUser'
}

export enum EnumLoginModule {
  'sign-in' = 'siginIn',
  'sign-up' = 'signUp',
  'reset' = 'reset',
  'verify-email' = 'verifyEmail',
  'forgot' = 'forgotPass',
}
